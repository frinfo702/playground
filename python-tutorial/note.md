# Memo

## Chapter 9

名前空間とは辞書で `x` という変数の中身は何なのか
`len()`という関数の中身が何なのか
などを保持している
`len()`などは組み込み名前空間に属し消えることはない

```py
def f():
  x = 1
  retun x
```

このような場合は`f()`が呼ばれた時点で、関数のローカル名前空間が作成され、処理中のみ`x: 10`という対応が`f()`の作った名前空間に保持される。関数の処理が終わると削除される。
